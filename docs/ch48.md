## 20. 编译器后端与计算映射 [新增]

>  **本章目标**：揭示编译器如何将 7 层循环嵌套的卷积/Attention 映射到 2D MAC 阵列——这是 NPU 设计中最核心的软件-硬件协同问题。

### 20.1 从算法到硬件：映射的本质

```mermaid
flowchart TB
    subgraph ALG["算法层 (7层循环嵌套)"]
        direction TB
        L1["for n in N (batch)"]
        L2["for oc in OC (输出通道)"]
        L3["for oh in OH (输出高度)"]
        L4["for ow in OW (输出宽度)"]
        L5["for ic in IC (输入通道)"]
        L6["for kh in KH (卷积核高)"]
        L7["for kw in KW (卷积核宽)"]
        L1 --> L2 --> L3 --> L4 --> L5 --> L6 --> L7
    end

    subgraph MAP["编译器映射决策"]
        direction TB
        M1["循环分块 (Tiling)"]
        M2["循环重排 (Reordering)"]
        M3["空间映射 (Spatial Mapping)"]
        M4["时间展开 (Unrolling)"]
        M1 --> M2 --> M3 --> M4
    end

    subgraph HW["硬件执行 (2D MAC阵列)"]
        direction TB
        H1["Tile 0<br/>16×16 MAC"]
        H2["Tile 1<br/>16×16 MAC"]
        H3["Tile 2<br/>16×16 MAC"]
        H4["Tile 3<br/>16×16 MAC"]
        H1 --- H2
        H3 --- H4
    end

    ALG -->|"编译器"| MAP -->|"配置"| HW

    style ALG fill:#dbeafe,stroke:#3b82f6,stroke-width:2px
    style MAP fill:#fef3c7,stroke:#f59e0b,stroke-width:2px
    style HW fill:#dcfce7,stroke:#22c55e,stroke-width:2px
```

### 20.2 Tiling 数学推导

Tiling 是映射的核心——决定每次计算多大的子问题。

假设: MAC阵列 R×C, SRAM 容量 S bytes, 带宽 BW bytes/s

卷积 GEMM 化后的矩阵乘法:  M × K  ×  K × N  =  M × N

**Tiling 约束条件**:

| 约束 | 公式 | 说明 |
|------|------|------|
| **SRAM容量** | Tile_A(Tm×Tk) + Tile_B(Tk×Tn) + Tile_C(Tm×Tn) + 累加器 ≤ S | Tile必须放入片上SRAM |
| **阵列尺寸** | Tm ≤ R, Tn ≤ C (或通过折叠映射) | Tile不能超过MAC阵列尺寸 |
| **带宽** | 数据加载时间 ≤ 计算时间 | 2×Tm×Tn×Tk / (R×C×freq) |

**优化目标**: 最大化 MAC利用率 = Tm×Tn / (R×C) × 数据复用因子

**实例**: ResNet-50 Conv3_1 (M=784, N=256, K=576), Tesla NPU (R=96, C=96, S=32MB):
→ Tm=96, Tn=96, Tk=576 → SRAM=14.9MB ✅ → 利用率=100%

### 20.3 CNN 卷积映射：im2col vs 直接卷积

```mermaid
flowchart LR
    subgraph IM2COL["方法1: im2col + GEMM"]
        direction TB
        I1["输入特征图<br/>(H×W×IC)"] -->|"展开(unfold)"| I2["im2col矩阵<br/>(OH×OW)×(KH×KW×IC)"]
        W1["权重<br/>(OC×KH×KW×IC)"] -->|"reshape"| W2["权重矩阵<br/>OC×(KH×KW×IC)"]
        I2 -->|"GEMM"| G["矩阵乘法<br/>OC×(OH×OW)"]
        W2 --> G
        G --> O1["输出特征图<br/>(OC×OH×OW)"]
    end

    subgraph DIRECT["方法2: 直接卷积映射"]
        direction TB
        D1["输入特征图<br/>(H×W×IC)"] -->|"直接映射<br/>IC→PE列<br/>OH×OW→PE行"| D3["MAC阵列<br/>直接卷积"]
        D2["权重<br/>(OC×KH×KW×IC)"] -->|"流式输入"| D3
        D3 --> D4["输出特征图<br/>(OC×OH×OW)"]
    end

    style IM2COL fill:#dbeafe,stroke:#3b82f6,stroke-width:2px
    style DIRECT fill:#dcfce7,stroke:#22c55e,stroke-width:2px
```

| 维度 | im2col + GEMM | 直接卷积映射 |
|------|---------------|-------------|
| **内存开销** | 高 (展开后矩阵是原来的 KH×KW 倍) | 低 (原地计算) |
| **MAC利用率** | 高 (GEMM 是 MAC 阵列的最优负载) | 中等 (受限于卷积参数) |
| **适用性** | 1×1, 3×3 标准卷积 | Depthwise, Group 卷积 |
| **Tesla NPU** | ✅ 使用 (网络架构适配) | ❌ |
| **地平线 BPU** | ⚠️ 混合使用 | ✅ 专用卷积映射 |
| **NVIDIA DLA** | ✅ 使用 | ⚠️ 部分支持 |

### 20.4 Attention 映射：FlashAttention 到脉动阵列

```mermaid
flowchart TB
    subgraph FA["FlashAttention Tiling 策略"]
        direction TB
        Q["Q: (seq_len × d)"]
        K["K: (seq_len × d)"]
        V["V: (seq_len × d)"]
        
        QT["Q_tile: (Br × d)<br/>Br = SRAM / (4×d)"]
        KT["K_tile: (Bc × d)<br/>Bc = SRAM / (4×d)"]
        VT["V_tile: (Bc × d)"]
        
        Q --> QT
        K --> KT
        V --> VT
        
        QT --> MUL["Q_tile × K_tile^T<br/>(Br × Bc)"]
        KT --> MUL
        MUL --> SM["在线 Softmax<br/>(数值稳定)"]
        SM --> MUL2["Attn × V_tile<br/>(Br × d)"]
        VT --> MUL2
        MUL2 --> OUT["O_tile: (Br × d)<br/>累加到输出"]
    end
    
    style FA fill:#f0f9ff,stroke:#1e40af,stroke-width:2px
    style MUL fill:#fee2e2,stroke:#ef4444
    style SM fill:#fef3c7,stroke:#f59e0b
    style MUL2 fill:#dcfce7,stroke:#22c55e
```

**FlashAttention 到 FSA 脉动阵列的映射**：

```mermaid
gantt
    title FlashAttention 在 16×16 FSA 阵列上的调度
    dateFormat X
    axisFormat %s

    section 加载 Q_tile
    DMA: Q → Scratch SRAM    :a1, 0, 2

    section 外循环 (K/V tiles)
    Load K_tile #0           :b1, 2, 4
    Q×K^T (MAC阵列)          :b2, 4, 8
    在线Softmax (Split单元)   :b3, 8, 10
    Load V_tile #0           :b4, 4, 6
    Attn×V (MAC阵列)         :b5, 10, 14
    累加到 O_tile             :b6, 14, 15

    section K/V tile #1
    Load K_tile #1           :c1, 15, 17
    Q×K^T                    :c2, 17, 21
    Softmax                  :c3, 21, 23
    Load V_tile #1           :c4, 17, 19
    Attn×V                   :c5, 23, 27
    累加                     :c6, 27, 28

    section 写回输出
    DMA: O → DDR             :d1, 28, 30
```

### 20.5 算子融合规则

```mermaid
flowchart LR
    subgraph BEFORE["融合前 (5次DDR访问)"]
        direction TB
        C1["Conv2D"] -->|"DDR写"| B1["BatchNorm"]
        B1 -->|"DDR写"| R1["ReLU"]
        R1 -->|"DDR写"| P1["MaxPool"]
    end

    subgraph AFTER["融合后 (1次DDR访问)"]
        direction TB
        F1["Fused<br/>Conv+BN+ReLU<br/>+MaxPool<br/>━━━━━━━━━<br/>SRAM内闭环<br/>零DDR中间结果"]
        F1 -->|"DDR写<br/>(仅最终结果)"| OUT["输出"]
    end

    BEFORE -->|"编译器优化"| AFTER

    style BEFORE fill:#fef2f2,stroke:#ef4444,stroke-width:2px
    style AFTER fill:#f0fdf4,stroke:#22c55e,stroke-width:2px
```

**常见融合模式**：

| 融合模式 | 适用模型 | SRAM需求节省 | 带宽节省 |
|---------|---------|-------------|---------|
| **Conv + BN + ReLU** | 所有CNN | 输出特征图大小 × 2 | ~3× |
| **Q·K^T + Softmax + ·V** | Attention | n² × 4 bytes | ~5-10× |
| **Linear + GELU** | Transformer FFN | 中间激活大小 | ~2× |
| **Conv + BN + ReLU + Pool** | CNN backbone | 输出 × 3 | ~4× |
| **BEV Pool + BEV Attn** | BEVFormer | BEV特征图 | ~3× |

<div class="callout callout-insight">

**融合的关键约束**：融合需要中间结果全部在 SRAM 中驻留。如果中间结果（如 Attention Map n²）超过 SRAM 容量，就无法完全融合——这正是 FlashAttention tiling 解决的问题。

</div>

### 20.6 数据复用分析

```mermaid
flowchart TB
    subgraph REUSE["三种数据复用模式"]
        direction TB
        
        subgraph WR["权重复用 (Weight Reuse)"]
            direction LR
            W1["权重固定在PE中"] --> W2["输入流动"]
            W2 --> W3["输出在PE累加"]
        end
        
        subgraph IR["输入复用 (Input Reuse)"]
            direction LR
            I1["输入固定在PE中"] --> I2["权重流动"]
            I2 --> I3["输出在PE累加"]
        end
        
        subgraph OR["输出复用 (Output Reuse)"]
            direction LR
            O1["输出固定在PE中"] --> O2["输入/权重流动"]
            O2 --> O3["部分和累加"]
        end
    end
    
    style WR fill:#fee2e2,stroke:#ef4444,stroke-width:2px
    style IR fill:#dbeafe,stroke:#3b82f6,stroke-width:2px
    style OR fill:#dcfce7,stroke:#22c55e,stroke-width:2px
```

| 工作负载 | 最佳复用模式 | 复用因子 | 每MAC所需带宽 |
|---------|------------|---------|-------------|
| **标准CNN (3×3 Conv)** | 输入复用 | ~9× | ~0.11 bytes/MAC |
| **1×1 Conv** | 权重复用 | ~H×W× | ~0.01 bytes/MAC |
| **Depthwise Conv** | 输出复用 | ~1× | ~1.0 bytes/MAC |
| **Attention Q×K^T** | 输入复用 (Q) | ~n× | ~4/n bytes/MAC |
| **Attention Attn×V** | 输入复用 (V) | ~n× | ~4/n bytes/MAC |

### 20.7 Timeloop 建模框架

Timeloop 是学术和工业界广泛使用的 DNN 加速器建模工具：

**Timeloop 建模流程**:

| 阶段 | 内容 |
|------|------|
| **输入** | 算法描述 (Conv/FC参数) \| 架构描述 (MAC阵列/SRAM/带宽) \| 映射约束 (数据流策略/Tile大小) |
| **Timeloop 分析** | 循环嵌套展开→空间/时间映射 \| 数据复用分析→带宽需求 \| MAC利用率计算 \| 能耗估算 (Accelergy+CACTI) |
| **输出** | 延迟(cycles) \| 能耗(mJ) \| 面积(mm²) \| MAC利用率(%) \| 带宽利用率(%) |

> **参考文献 [P28]**: Parashar, A., et al. "Timeloop: A Systematic Approach to DNN Accelerator Evaluation." ISPASS 2020.

> **参考文献 [P29]**: Yang, X., et al. "Interstellar: Using Halide's Scheduling to Explore DNN Accelerator Design Spaces." DAC 2020.

> **参考文献 [P30]**: Niu, W., et al. "SODA: A Full-Stack DNN Compiler and Accelerator Generator." DAC 2023.

---